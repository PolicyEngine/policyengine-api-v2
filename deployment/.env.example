###############################################################################
# OPTION 1: Using EXISTING GCP project (recommended for most users)
###############################################################################
# Set your existing project ID here:
PROJECT_ID=your-existing-project-id
GOOGLE_CLOUD_PROJECT=${PROJECT_ID}  # Should match PROJECT_ID
REGION=us-central1
REPO=api-v2

###############################################################################
# OPTION 2: Create NEW GCP project via terraform (advanced)
###############################################################################
# Uncomment and fill these ONLY if you want terraform to create a new project:
# TF_VAR_org_id=your-org-id  # Get from: gcloud organizations list
# TF_VAR_billing_account=your-billing-account  # Get from: gcloud billing accounts list
# TF_VAR_project_name=policyengine-api  # Base name for project
# TF_VAR_github_repo_owner_id=your-github-org  # GitHub organization
# TF_VAR_github_repo=policyengine-api-v2  # GitHub repo name
# TF_VAR_github_repo_ref=main  # Branch to deploy from

###############################################################################
# Infrastructure configuration (required for all)
###############################################################################
# Terraform Variables (auto-populated from above)
TF_VAR_project_id=${PROJECT_ID}
TF_VAR_region=${REGION}
TF_VAR_stage=dev  # dev, staging, or prod
TF_VAR_is_prod=false  # true for production
TF_VAR_full_container_tag=latest
TF_VAR_simulation_container_tag=latest
TF_VAR_tagger_container_tag=latest

# Build configuration
TAG=latest
ENVIRONMENT=desktop  # desktop or production

# Optional: Slack notifications
# TF_VAR_slack_notification_channel_name=your-slack-channel